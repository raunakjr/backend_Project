<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile Card</title>
    <style>
      body {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        background-color: #2c2c2c;
        margin: 0;
        font-family: Arial, sans-serif;
      }

      .card {
        background: #fff;
        width: 350px;
        border-radius: 15px;
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        overflow: hidden;
        text-align: center;
        padding: 20px;
      }

      .image {
        width: 120px;
        height: 120px;
        border-radius: 50%;
        background: aquamarine;
        margin: 0 auto;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 20px;
        color: #333;
        font-weight: bold;
      }

      .details {
        margin-top: 15px;
      }

      .details p {
        margin: 5px 0;
        color: #444;
        font-size: 16px;
      }

      .username {
        font-weight: bold;
        font-size: 18px;
        color: #007bff;
      }

      .email {
        font-size: 14px;
        color: #777;
      }

      .buttons {
        margin-top: 15px;
        display: flex;
        justify-content: center;
        gap: 10px;
      }

      .btn {
        padding: 10px 15px;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        transition: 0.3s;
        font-size: 14px;
      }

      .edit-btn {
        background: #28a745;
        color: white;
      }

      .edit-btn:hover {
        background: #218838;
      }

      .delete-btn {
        background: #dc3545;
        color: white;
      }

      .delete-btn:hover {
        background: #c82333;
      }
      .No_Users {
        width: 50%;
        height: 50%;
        display: flex;
        flex-direction: column;
        justify-content: center;
        padding: 40px;
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
      }
      .No_Users p {
        font-family: "Franklin Gothic Medium", "Arial Narrow", Arial, sans-serif;
        font-size: xx-large;
      }
    </style>
  </head>
  <body>
    <% if(!allusers || allusers.length==0 ){ %>
    <div class="No_Users">
      <p>No users found</p>
      <a href="/">Go back</a>
    </div>
    <% } %> <% allusers.forEach((user)=>{%>
    <div class="card">
      <div class="image">
        <img class="image" src="<%= user.image %>" alt="User Image" />
      </div>
      <div class="details">
        <p class="username"><%=user.name%></p>
        <p class="email"><%=user.email%></p>
      </div>
      <div class="buttons">
        <a href="/delete/<%=user._id%>" class="btn delete-btn">Delete User</a>
        <a href="/edit/<%=user._id%>" class="btn edit-btn">Edit User</a>
      </div>
    </div>
    <% })%>
  </body>
</html>
